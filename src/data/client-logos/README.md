# Client logos (content)

Each subdirectory is one Keystatic entry (`index.yaml` + `logo.png`).

- Edit in the CMS at `/admin` → **Home — Client logos**, or edit YAML here.
- **order**: lower numbers appear first (e.g. 10, 20, 30).
- **draft**: hide a logo without deleting it.
